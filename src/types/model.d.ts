export type ProjectType = {
  name: string;
  logo: string;
  shortName?: string;
  id: number;
};

export type ProjectDetailType = {
  name: string;
  description: string;
  shortName: string;
  logo: string;
  id: number;
};
